<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title>編集: <%= data.name %></title>
  <style>
    body { font-family: sans-serif; background-color: #f3e5f5; padding: 20px; }
    .container { background-color: white; padding: 30px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); max-width: 500px; margin: 0 auto; }
    h2 { color: #6A0DAD; }
    label { font-weight: bold; display: block; margin-top: 15px; }
    input[type="text"] { width: 100%; padding: 8px; margin-top: 5px; box-sizing: border-box; border: 1px solid #ccc; border-radius: 4px; }
    input[type="submit"] { background-color: #6A0DAD; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer; margin-top: 20px; width: 100%; font-size: 16px; }
    input[type="submit"]:hover { background-color: #4a097a; }
    .back { display: block; text-align: center; margin-top: 15px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <h2>情報の編集</h2>
    <form action="/sf6/update/<%= data.id %>" method="POST">
      
      <label>名前</label>
      <input type="text" name="name" value="<%= data.name %>">
      
      <label>バトルタイプ</label>
      <input type="text" name="type" value="<%= data.type %>">
      
      <label>身長</label>
      <input type="text" name="height" value="<%= data.height %>">
      
      <label>誕生日</label>
      <input type="text" name="birth" value="<%= data.birth %>">

      <label>体重</label>
      <input type="text" name="weight" value="<%= data.weight %>">

      <label>好きなもの</label>
      <input type="text" name="likes" value="<%= data.likes %>">

      <label>嫌いなもの</label>
      <input type="text" name="dislikes" value="<%= data.dislikes %>">
      
      <input type="submit" value="更 新">
    </form>
    <a href="/sf6" class="back">キャンセル</a>
  </div>
</body>
</html>