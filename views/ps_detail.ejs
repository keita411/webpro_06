<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title><%= data.name %>の詳細</title>
  <style>
    body { font-family: sans-serif; background-color: #f9f9f9; padding: 20px; }
    .container { background-color: white; padding: 30px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); max-width: 600px; margin: 0 auto; }
    h2 { color: #00439C; border-left: 5px solid #00439C; padding-left: 10px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th { background-color: #eee; text-align: left; padding: 10px; width: 30%; border-bottom: 1px solid #ddd; }
    td { padding: 10px; border-bottom: 1px solid #ddd; }
    .btn-group { margin-top: 30px; text-align: center; }
    .btn { padding: 10px 20px; margin: 0 5px; text-decoration: none; border-radius: 4px; color: white; display: inline-block; }
    .edit { background-color: #4CAF50; }
    .del { background-color: #f44336; }
    .back { background-color: #888; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <h2><%= data.name %></h2>
    <table>
      <tr><th>ID</th><td><%= data.id %></td></tr>
      <tr><th>ハード名称</th><td><%= data.name %></td></tr>
      <tr><th>ハード販売数</th><td><%= data.hard %></td></tr>
      <tr><th>ソフト販売数</th><td><%= data.soft %></td></tr>
      <tr><th>発売日</th><td><%= data.date %></td></tr>
    </table>

    <div class="btn-group">
      <a href="/ps/edit/<%= data.id %>" class="btn edit">編集</a>
      <a href="/ps/delete/<%= data.id %>" class="btn del">削除</a>
      <br>
      <a href="/ps" class="btn back">一覧に戻る</a>
    </div>
  </div>
</body>
</html>